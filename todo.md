Initial scan from figma:

header (space between)
   logo
   links
   icons
container (depends on page)

homepage container
   main content
      filters (space between)
         sort by dropdown (price / title)
         showing x results
      products grid
         product item
            prod image
            prod title
            prod price
      pagination
   sidebar (categories) (240px fixed width)
      category

prod detail container
   main content (image)
   sidebar (product info) (400px fixed width)
      prod title
      prod price
      add to cart button
      hr
      prod description

shopping cart container
   main content (cart products list)
   sidebar (cart summary) (400px fixed width)

-----------------------

TYPES

product
   id
   title
   price
   image
   description
   category

cart-item
   id
   qty

